# Class 18 - Search

Example of searching a value in an array in C.

## Files

- **[main.c](./main.c)**: Finds a value entered by the user in a sorted array using binary search. Prints the array first, and reports the position found or that the value is not present.

## Build

```sh
gcc main.c -o output/main.exe
```
